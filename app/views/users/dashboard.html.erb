<center><h1>User Dashboard</h1><center>
<br>
<div align =center>
	<div style= "width: 50%" align="center">
		<b style="text-align:left;">Recent password leaks for your account:</b>
		<ul style="text-align:left;">
			<% if @leaks.nil? or @leaks.empty? %>
				<li align=left> 
					Congratulations, there have been no leaks so far for your accounts (that we know of!)
				</li>
			<% else %>
				<% @leaks.each do |leak| %>
					<li align=left>
						<% leak_news = FeedEntry.find(Leak.find_by_service_id(leak).feed_entry_id) %> 
						<%= link_to "#{leak.url} had a leak recently. Have you changed your password yet?", leak_news.url %> - <%=leak_news.published_at.strftime('%m/%d/%Y') %>
					</li>
				<% end %>
			<% end %>
		</ul>
	</div>
<div>